import { Fragment } from "react";
import { Form } from "../components/Form";

export const Create = () => {
	return (
		<Fragment>
			<h1 className="text-center">Create note</h1>
			<Form />
		</Fragment>
	)
}